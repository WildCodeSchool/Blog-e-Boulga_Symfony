{% extends 'admin/base_admin.html.twig' %}

{% block stylesheets %}{{ encore_entry_link_tags('admin_category') }}{% endblock %}

{% block javascripts %}{{ encore_entry_script_tags('admin_category') }}{% endblock %}

{% block title %}Edition de votre Categorie{% endblock %}

{% block body %}
    <main>
    <h1>Edition de votre Categorie</h1>

        <div class="buttonCat">
    {{ include('admin/category/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
        <div class="buttonCat">
    {{ include('admin/category/_delete_form.html.twig') }}
        </div>
        <div class="buttonCat">
        <a href="{{ path('app_category_index') }}">retour</a>
        </div>
    </main>
{% endblock %}
